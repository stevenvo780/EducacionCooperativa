import FormalizerPlayground from '@/components/FormalizerPlayground';

export const metadata = {
  title: 'Formalizador ST — Autologic',
  description: 'Herramienta de formalización de texto natural a lógica formal ST'
};

export default function FormalizePage() {
  return <FormalizerPlayground />;
}
