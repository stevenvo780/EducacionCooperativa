{
  "rules": {
    "sync-events": {
      "$workspaceId": {
        ".read": "auth != null && (!$workspaceId.beginsWith('personal_') || $workspaceId === 'personal_' + auth.uid)",
        ".write": "auth != null && (!$workspaceId.beginsWith('personal_') || $workspaceId === 'personal_' + auth.uid)",
        ".indexOn": ["timestamp"],
        "$eventId": {
          ".validate": "newData.hasChildren(['type', 'path', 'timestamp'])"
        }
      }
    },
    "presence": {
      "$wsId": {
        ".read": "auth != null && (!$wsId.beginsWith('personal_') || $wsId === 'personal_' + auth.uid)",
        ".write": "auth != null && (!$wsId.beginsWith('personal_') || $wsId === 'personal_' + auth.uid)"
      }
    }
  }
}