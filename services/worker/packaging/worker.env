# /etc/edu-worker/worker.env
# =====================================================
# WORKER CONFIGURATION - PRODUCTION
# =====================================================

NEXUS_URL=http://148.230.88.162:3010
WORKER_IMAGE=ghcr.io/educacioncooperativa/worker:latest

# Firebase Configuration
FIREBASE_BUCKET=udea-filosofia.firebasestorage.app

# Service account file path (the JSON file will be mounted into containers)
SERVICE_ACCOUNT_PATH=/etc/edu-worker/serviceAccountKey.json

# Base path for workspace mounts - SET BY POSTINST ON INSTALL
BASE_MOUNT_PATH=
